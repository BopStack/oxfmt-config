# Changelog

## 0.2.0

- Rename `.oxfmtrc.json` → `oxfmtrc.json` (drop dot prefix).
- Add `exports["."]` and `"main"` so `"extends": "@bopstack/oxfmt-config"` works.

## 0.1.0

- Initial release: shared `oxfmtrc.json` package as `@bopstack/oxfmt-config`.
