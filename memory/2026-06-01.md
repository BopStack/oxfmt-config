# 2026-06-01

- Prepared `@bopstack/oxfmt-config` for publishing from `/Users/bruno/Projects/bopstack/oxfmt`.
- Created and pushed public GitHub repo `BopStack/oxfmt-config`.
- Remote normalized to HTTPS: `https://github.com/BopStack/oxfmt-config.git`.
- Package publishes `oxfmtrc.json`, `CHANGELOG.md`, `LICENSE`, `README.md`, and `package.json`.
- Release gate used: `just check` and `just pack`.
